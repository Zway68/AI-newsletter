name: ai-newsletter
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - fastapi
    - uvicorn
    - pytest
    - requests
    - pydantic
    - python-dotenv
    # Add other backend dependencies here
